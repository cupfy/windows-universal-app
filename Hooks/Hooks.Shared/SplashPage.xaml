<Page
    x:Class="Hooks.SplashPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Hooks"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    RequestedTheme="Dark" 
    Background="{StaticResource AppMainBrush}">

    <Grid x:Name="LayoutRoot">
        <!--Canvas>
            <Image Source="Assets/SplashScreen.png" Width="{Binding ActualWidth, ElementName=LayoutRoot, Mode=OneWay}"/>
        </Canvas>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="7*"/>
                <RowDefinition Height="5*"/>
                <RowDefinition Height="8*"/>
            </Grid.RowDefinitions>-->
                
            <ProgressRing Grid.Row="2" IsActive="True" Width="20" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <!--</Grid>-->
    </Grid>
</Page>
